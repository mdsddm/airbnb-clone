# Copy this to a file named .env locally and set real values
# Example values (do NOT commit your real secrets to git)
DB_URI=
CLOUD_NAME=
CLOUD_API_KEY=
CLOUD_API_SECRET=
SESSION_SECRET=
# Optional: set NODE_ENV=production in your host environment
